# AI-Model-Router
A lightweight Streamlit-based interface that sends user queries to a backend router API, which intelligently selects the most suitable AI model to generate responses. This project provides a simple, user-friendly UI for interacting with multiple AI models through a unified endpoint.
